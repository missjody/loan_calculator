// generates a slider based off of parameters provided, only visible if loan type has been selected

// The limits will be dependent on what type of loan is selected -- build in that functionality

import React from "react";

const Slider = () => (
    <div>
        Test Slider
    </div>
    
    // the slider needs to be updated to reflect the number onto the slider directly
    // <p> You want to borrow ${userEntry.loanAmount}</p>
    // <input type="range" step={500} max={65000} min={500} name="loanAmount" value={userEntry.loanAmount} onChange={handleInputChange} />
)

export default Slider